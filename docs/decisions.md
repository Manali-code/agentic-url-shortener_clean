# Decisions

- Use FastAPI for lightweight HTTP API delivery
- Use SQLAlchemy for persistence abstraction across SQLite and Postgres
- Use LangGraph/LangChain patterns for agent workflow representation
- Keep release gating explicit rather than implicit
- Treat the project as both a product scaffold and an orchestration example
